apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "gen3kubernetes.labels" . | nindent 4 }}
    component: "service-guppy"
  name: service-guppy-{{ .Values.ENV }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.guppy.serviceport }}
    protocol: TCP
  selector:
    {{- include "gen3kubernetes.selectorLabels" . | nindent 4 }}
    component: "guppy"
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
