apiVersion: v1
kind: Service
metadata:
  name: {{ include "gen3kubernetes.fullname" . }}
  labels:
    {{- include "gen3kubernetes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gen3kubernetes.selectorLabels" . | nindent 4 }}
