apiVersion: v1
kind: Service
metadata:
  labels:
    {{- include "gen3kubernetes.labels" . | nindent 4 }}
    component: "service-jupyter"
  name: service-jupyter-{{ .Values.ENV }}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: {{ .Values.jupyter.serviceport }}
    protocol: TCP
  selector:
    {{- include "gen3kubernetes.selectorLabels" . | nindent 4 }}
    component: "jupyter"
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
